spring:
  config:
    import: "classpath:application-common.yml"
  application:
    name: payment-service
  datasource:
    url: jdbc:sqlserver://${DB_HOST:localhost}:${DB_PORT:1433};databaseName=chinook_payment;encrypt=false

server:
  port: 8084
